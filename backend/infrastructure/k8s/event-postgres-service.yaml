apiVersion: v1
kind: Service
metadata:
  name: event-postgres
  namespace: sharks
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      name: postgres
  selector:
    app: event-postgres
